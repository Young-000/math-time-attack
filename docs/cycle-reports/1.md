# Cycle 1 Report — appLogin + mTLS 인증 인프라 구축

## 결과: COMPLETED

**PR:** https://github.com/Young-000/math-time-attack/pull/8 (squash merged)

## 완료 항목

### 클라이언트
- `userIdentity.ts`: getUserKeyForGame → appLogin 기반으로 전환
- TTL 기반 localStorage 캐시 (50분, 토큰 만료 60분 대비)
- 비AIT 환경 localStorage fallback 유지
- `VITE_MOCK_USER_KEY` 개발 모드 지원

### Supabase Edge Function (`auth/`)
- `index.ts`: 메인 핸들러 (요청 검증, 토큰 교환, 세션 저장, 에러 매핑)
- `_shared/toss-api-client.ts`: mTLS 토스 파트너 API 클라이언트 (Deno.createHttpClient)
- `_shared/types.ts`: 공유 타입 + 에러 코드 상수
- `_shared/cors.ts`: CORS 유틸리티

### DB 마이그레이션
- `user_sessions` 테이블 (user_key, access_token, refresh_token, token_expires_at)
- RLS: service_role만 접근 가능
- 인덱스: user_key (UNIQUE), token_expires_at

### 테스트
- `userIdentity.test.ts`: 20+ 단위 테스트
  - AIT 환경: appLogin 성공 → Edge Function → userKey
  - AIT 환경: appLogin 실패 → fallback
  - AIT 환경: Edge Function 에러 → fallback
  - 캐시: localStorage TTL 히트/만료
  - 비AIT 환경: fallback ID 반환
  - getUserId: 캐시 우선, 미캐시 시 초기화

### 문서
- `docs/specs/mtls-auth-infra.md`: PM 스펙 (822줄)
- `docs/cycle-brief.md`: 개발자 브리프

## 품질 게이트

| 항목 | 결과 |
|------|------|
| tsc --noEmit | 통과 |
| eslint | 에러 0 |
| vitest | 279/279 통과 |
| vite build | gzip 73KB (< 500KB) |

## 미완료 / 다음 사이클

- Edge Function 실제 배포 (mTLS 인증서 환경변수 설정 필요)
- AIT 환경 통합 테스트 (Cycle 4에서 진행)
- RefreshToken 갱신 로직 (Cycle 4)

## 리스크/이슈

1. **Deno.createHttpClient mTLS**: unstable API. Supabase Edge Function 프로덕션에서 검증 필요 (스파이크 pending)
2. **appLogin SDK 호환성**: 비게임 카테고리에서 정상 동작 검증 필요 (AIT 개발환경 테스트 pending)
3. **기존 데이터 마이그레이션**: getUserKeyForGame hash → appLogin userKey 전환은 Cycle 3에서 처리

## 시간

- Phase 1 (PM): 이전 세션에서 완료
- Phase 2 (Dev): 이전 세션에서 완료
- Phase 3 (QA): 테스트 279개 전부 통과 확인
- Phase 4 (PD): UI 변경 없음 (인프라 전용) — 스킵
- Phase 5 (Merge): PR #8 squash merge 완료
