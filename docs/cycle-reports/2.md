# Cycle 2 Report — 비게임 프로모션 서버 API 구현

## 결과: COMPLETED

**PR:** https://github.com/Young-000/math-time-attack/pull/9 (squash merged)

## 완료 항목

### Edge Function `promotion/`
- `index.ts`: 메인 핸들러 (요청 검증, 중복 체크, 프로모션 플로우 실행, DB 기록)
- `_shared/promotion-client.ts`: mTLS 3단계 플로우 (get-key → execute → result polling)
- `_shared/types.ts`: 프로모션 API 타입 + 에러 코드

### 클라이언트
- `promotionService.ts`: grantPromotionRewardForGame 제거, Edge Function 호출로 전환
- `DifficultySelectPage.tsx`: userKey 전달 업데이트

### DB 마이그레이션
- `promotion_records` 테이블 (user_key, promotion_code, amount, status 등)
- UNIQUE(user_key, promotion_code) 서버 사이드 중복 방지
- RLS: service_role만 접근

### 테스트
- `promotionService.test.ts`: 11 케이스 (성공/실패/중복/네트워크에러/비AIT)

## 품질 게이트

| 항목 | 결과 |
|------|------|
| tsc --noEmit | 통과 |
| eslint | 에러 0 |
| vitest | 290/290 통과 |
| vite build | gzip 73.53KB (< 500KB) |
